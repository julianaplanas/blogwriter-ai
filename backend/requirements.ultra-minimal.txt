# Ultra-minimal requirements avoiding Rust dependencies
# Use Pydantic v1 to avoid Rust compilation

fastapi==0.68.0
uvicorn==0.15.0
pydantic==1.10.13
requests==2.31.0
python-dotenv==1.0.0

# Try without these first
# groq==0.4.1  
# aiosqlite==0.19.0
