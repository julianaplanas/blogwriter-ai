# Step 3: Add basic AI dependencies
# Building on successful step2 deployment

# Core (working)
fastapi==0.68.0
uvicorn==0.15.0
requests
python-dotenv==1.0.0

# Add database support with built-in sqlite3
# (avoiding aiosqlite which might have dependencies)

# Try adding groq with compatible versions
# groq package might work with older fastapi/pydantic
groq==0.4.1
